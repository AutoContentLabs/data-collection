CREATE TABLE languages (
    language_code VARCHAR(5) NOT NULL PRIMARY KEY,  -- Code for the language (e.g., 'EN' for English)
    language_name VARCHAR(50) NOT NULL              -- Full name of the language (e.g., 'English')
);
